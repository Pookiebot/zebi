module.exports = class {
  constructor(client) {
    this.client = client;
  }

  async run(member) {
    //const channel = member.guild.channels.get("696759097864749076");
    //channel.setName(`En ligne  :  ${member.guild.members.filter(m => m.presence.status != 'offline').size -1}`)
  }
};